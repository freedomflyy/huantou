# 幻头项目执行任务清单

最后更新：`2026-03-13`

## 1. 使用规则

- 每次你说“继续”，我会更新一次本文件。
- `状态` 取值：`待办` / `进行中` / `已完成` / `阻塞`。
- 默认优先级：后端稳定性 > 安全性 > 管理能力 > 演示层。

## 2. 当前总任务表

| 编号 | 任务 | 状态 | 备注 |
|---|---|---|---|
| T1 | 后端核心数据与任务链路（登录/积分/任务/资产） | 已完成 | 已可真实出图与入库 |
| T2 | 火山引擎三类生成能力接入 | 已完成 | txt2img/img2img/style_transfer |
| T3 | 基础编辑 quick_edit（裁剪/旋转/亮度等） | 已完成 | 已接入任务执行 |
| T4 | 收藏系统（收藏/取消/收藏列表） | 已完成 | 已上线 API |
| T5 | 审核接入主流程（文本/输入图/输出图） | 已完成 | 支持 mock / tencent(COS/CI) |
| T6 | 结果图落本地/COS（去外链依赖） | 已完成 | 生成结果已下载后再入存储 |
| T7 | 管理后台 API（用户/积分/任务重试/作品下架） | 已完成 | 含简易 Web 控制台 |
| T8 | 异步任务消费 Worker（queued -> execute） | 已完成 | 基础轮询 worker 已完成 |
| T9 | JWT 鉴权（access/refresh） | 已完成 | 登录/刷新/接口校验均已落地 |
| T10 | Refresh token 撤销与轮换安全 | 已完成 | 轮换、单点登出、全量登出已落地 |
| T11 | 生产安全收口（关闭调试通道、策略固化） | 已完成 | 已加 prod 启动安全校验与禁用策略 |
| T12 | 审核回调入库闭环（异步审计） | 已完成 | 回调入库 + 审计记录 + 自动下架联动 |
| T13 | 可观测性（结构化日志/错误追踪） | 已完成 | 已接入请求指标、限流指标、Sentry 可配置初始化 |
| T14 | 前端（小程序功能测试版） | 已完成 | 已覆盖登录/积分/任务/资产收藏/管理与观测全链路 |
| T15 | 后端全链路 Demo 验证脚本与实测 | 已完成 | `scripts/demo_full_flow.py`，本轮实测 18/18 通过 |

## 3. 本次执行记录（2026-03-13）

- 完成：新增执行任务清单文件并纳入迭代管理。
- 完成：JWT 登录/刷新链路（`access + refresh`）及访问鉴权切换。
- 完成：`refresh_tokens` 表 + 轮换撤销（refresh 旋转、`/auth/logout`、`/auth/logout-all`）。
- 完成：生产环境鉴权安全开关（可强制禁用 `X-User-Id` 与 mock token）。
- 完成：生产环境启动安全校验（弱密钥/调试通道误配置直接阻断启动）。
- 完成：审核回调闭环（`/moderation/tencent/callback` 入库、审核审计、命中自动下架资产）。
- 完成：新增过期 refresh token 清理脚本。
- 完成：可观测性基础设施（结构化请求日志、`X-Request-Id`、全局异常日志）已上线。
- 完成：可观测性增强（请求统计、`/health/metrics` 管理端观测接口、限流命中统计）。
- 完成：API 限流中间件（全局 + `/auth/*` + `/tasks/*/execute`，含 `Retry-After` 响应头）。
- 完成：Sentry 初始化流程（配置 `SENTRY_DSN` 即启用；未安装 SDK 时降级告警不阻断服务）。
- 完成：`bs` 环境安装 `sentry-sdk==2.35.0`，可直接启用错误追踪。
- 完成：新增后端全链路 Demo 脚本 `backend/scripts/demo_full_flow.py`（登录/积分/四类任务/收藏/管理端/审核回调/指标）。
- 完成：全链路 Demo 实测（`ADMIN_API_KEY=demo-admin-key ... --ai-provider auto`）18/18 通过，报告已输出到 `backend/reports/demo_full_flow_latest.json`。
- 完成：小程序前端功能测试版（页面已扩展到登录、首页导航、任务创建执行、任务列表、资产与收藏、积分流水、管理与观测、审核回调模拟）。
- 下一步：按你指令推进小程序美工/UI 高保真重构，并继续补异步队列增强（Redis/Celery）与备份恢复策略。
